spring:
  cloud:
    config:
      fail-fast: true
      label: ${spring.profiles.active}
      profile: ${spring.profiles.active}
      discovery:
        service-id: paascloud-config
        enabled: true

