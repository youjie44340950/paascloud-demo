spring:
  cloud:
    config:
      fail-fast: true
      discovery:
        service-id: paascloud-config
        enabled: true
      label: ${spring.profiles.active}
      profile: ${spring.profiles.active}
